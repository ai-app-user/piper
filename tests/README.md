# Piper Tests

Piper-specific unit tests currently live in the integrated Hypersync test
binary while the pipeline library is being separated. New tests for reusable
queue, buffer, transport, and generic-job behavior should move here as the
library gains its own standalone test binary.
